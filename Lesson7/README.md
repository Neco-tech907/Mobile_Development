# Отчёт по практической работе №7

**Подготовил:** студент группы БСБО-09-23  
**ФИО:** Иванов Раул Рашадович

---

# 1. Цель работы

Целью практической работы является изучение сетевого взаимодействия в Android-приложениях, получение навыков работы с сокетами, встроенным HTTP-клиентом `HttpURLConnection` и внешней облачной платформой Firebase.

В ходе выполнения работы были изучены:

- получение данных по сети через `Socket`;
- выполнение HTTP-запросов через `HttpURLConnection`;
- обработка и разбор JSON-ответов;
- получение внешнего IP-адреса устройства;
- использование координат для запроса погоды;
- работа с `Firebase Authentication`;
- регистрация, вход и верификация пользователя по email.

---

# 2. Структура проекта

В проекте используются следующие модули:

1. `timeservice` — приложение для получения точного времени по сети.
2. `httpurlconnection` — приложение для определения внешнего IP-адреса, геоданных и текущей погоды.
3. `firebaseauth` — приложение для регистрации, авторизации и верификации пользователя через Firebase.
4. `app` — шаблонный модуль Android Studio, не задействованный в основной части практической работы.

---

# 3. Выполнение практической работы

# 3.1. Получение сетевого времени (модуль `timeservice`)

В модуле `timeservice` реализовано получение точного времени с удалённого сервера. Первоначально для этого использовалось подключение к `time.nist.gov` по сокету на порту `13`, однако в процессе тестирования было выявлено, что на некоторых устройствах этот сервис может возвращать пустой ответ. Поэтому в приложение был добавлен дополнительный надёжный сценарий получения времени через HTTPS.

В модуле были реализованы:

- работа с сокетами через `Socket`;
- чтение данных через `BufferedReader`;
- обработка сетевых ошибок и таймаутов;
- резервное получение времени по HTTP `Date`-заголовку;
- вывод даты и времени на экран.

При нажатии на кнопку приложение выполняет запрос к серверу времени и отображает результат в виде отдельных строк `Дата` и `Время`.

## Демонстрация работы

> Рисунок 1 - Получение времени в модуле `timeservice`
<img width="306" height="425" alt="Screenshot_38" src="https://github.com/user-attachments/assets/5debff90-c2bc-47bb-8473-90e15110bbfb" />


---

# 3.2. Определение внешнего IP и получение погоды (модуль `httpurlconnection`)

Во втором модуле реализована цепочка сетевых запросов через `HttpURLConnection`. Сначала приложение обращается к сервису `https://ipinfo.io/json`, получает внешний IP-адрес устройства и сопутствующие данные о местоположении. После этого из поля `loc` извлекаются координаты, которые передаются в сервис `Open-Meteo` для получения погодной информации.

В модуле были реализованы:

- HTTP GET-запросы через `HttpURLConnection`;
- чтение ответа сервера через `InputStream`;
- разбор JSON-ответа через `JSONObject`;
- вывод на экран данных:
  - внешний IP;
  - город;
  - регион;
  - страна;
  - провайдер;
  - почтовый индекс;
  - часовой пояс;
  - координаты;
- запрос текущей погоды по координатам;
- отображение температуры, скорости ветра и кода погоды.

Приложение также проверяет наличие интернет-соединения через `ConnectivityManager` и выводит сообщение пользователю, если сеть недоступна.

## Демонстрация работы

> Рисунок 2 - Получение внешнего IP-адреса и геоданных, текущей погоды по координатам
<img width="343" height="760" alt="Screenshot_39" src="https://github.com/user-attachments/assets/22736ca3-4164-4b4f-ba45-4583c5affcc2" />

---

# 3.3. Аутентификация через Firebase (модуль `firebaseauth`)

В модуле `firebaseauth` реализована полноценная работа с Firebase Authentication. Пользователь может создать аккаунт по email и паролю, войти в приложение, выйти из аккаунта и отправить письмо для подтверждения электронной почты.

В процессе выполнения задания были изучены:

- подключение Firebase к Android-проекту;
- использование `google-services.json`;
- инициализация `FirebaseAuth`;
- регистрация пользователя через `createUserWithEmailAndPassword()`;
- вход пользователя через `signInWithEmailAndPassword()`;
- отправка письма подтверждения через `sendEmailVerification()`;
- отображение `UID` пользователя и статуса `verified`.

В модуле были добавлены:

- валидация введённых email и пароля;
- сохранение сессии пользователя между запусками приложения;
- обработка и вывод текстов ошибок Firebase;
- разрешение `INTERNET` в `AndroidManifest.xml`.

После успешной регистрации пользователь отображается в интерфейсе приложения, а также появляется в консоли Firebase Authentication.

## Демонстрация работы

> Рисунок 4 - Экран регистрации и входа пользователя
<img width="346" height="757" alt="Screenshot_40" src="https://github.com/user-attachments/assets/2b2abdd4-aeaa-48ba-a8ff-fb988eae6c12" />


> Рисунок 5 - Успешная авторизация и отображение UID пользователя
<img width="339" height="758" alt="Screenshot_42" src="https://github.com/user-attachments/assets/f34262fc-ded0-4044-8b16-55c7cef5b519" />


> Рисунок 6 - Подтверждение email через письмо Firebase
<img width="822" height="551" alt="Screenshot_41" src="https://github.com/user-attachments/assets/3116dc3a-125a-4a83-a679-f5e7ab43c828" />


---

# 4. Итоги практической работы

В ходе выполнения практической работы были изучены основные способы сетевого взаимодействия в Android-приложениях и их практическое применение.

В процессе выполнения работы были получены практические навыки:

- работы с сокетами в Android;
- выполнения HTTP-запросов через `HttpURLConnection`;
- чтения и обработки JSON-ответов;
- получения внешнего IP-адреса и геоданных устройства;
- работы с погодными API;
- интеграции Firebase в Android-проект;
- регистрации и аутентификации пользователей;
- отправки письма верификации на email.

Итогом работы стало создание набора Android-приложений, демонстрирующих разные подходы к сетевому взаимодействию: от низкоуровневых сокетов до REST API и облачной аутентификации через Firebase.
